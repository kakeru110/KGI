# Copy this file to .env.local and fill in real values once Beds24 API
# credentials are available. Until BEDS24_API_KEY is set, the site runs
# entirely on deterministic dummy data (see src/lib/beds24/client.ts).

BEDS24_API_KEY=
